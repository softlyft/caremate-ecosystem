{
  "name": "caremate-ecosystem",
  "version": "0.1.0",
  "private": true,
  "description": "CareMate monorepo — mobile, admin portal, shared Supabase schema",
  "workspaces": [
    "packages/*",
    "caremate",
    "caremate-portal",
    "caremate-provider-portal",
    "payment",
    "website"
  ],
  "scripts": {
    "supabase:link": "supabase link --project-ref eybakmhqtotoywwgwgjy",
    "supabase:migration:new": "supabase migration new",
    "supabase:migration:list": "supabase migration list",
    "supabase:db:push": "supabase db push",
    "db:types": "node scripts/merge-db-types.mjs",
    "portal:dev": "npm run dev -w caremate-portal",
    "portal:build": "npm run build -w caremate-portal",
    "provider-portal:dev": "npm run dev -w caremate-provider-portal",
    "provider-portal:build": "npm run build -w caremate-provider-portal",
    "payment:dev": "npm run dev -w caremate-payment",
    "payment:build": "npm run build -w caremate-payment",
    "website:dev": "npm run dev -w caremate-website",
    "website:build": "npm run build -w caremate-website",
    "website:preview": "npm run preview -w caremate-website",
    "mobile:start": "npm run start -w caremate",
    "mobile:android": "npm run android -w caremate",
    "mobile:apk": "npm run android:apk -w caremate",
    "mobile:ios": "npm run ios -w caremate",
    "ingest:dev": "cd provider-ingestion && (test -d .venv || python3.12 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8090",
    "ingest:test": "cd provider-ingestion && (test -x .venv/bin/python || python3.12 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/pytest --cov=app --cov-report=term-missing",
    "lint": "npm run lint -w caremate && npm run lint -w caremate-portal && npm run lint -w caremate-provider-portal",
    "typecheck": "npm run typecheck -w caremate && npm run typecheck -w caremate-portal && npm run typecheck -w caremate-provider-portal && npm run typecheck -w caremate-payment && npm run typecheck -w caremate-website",
    "test": "npm run test -w caremate && npm run test -w caremate-portal && npm run test -w caremate-payment && npm run ingest:test && npm run test:email-templates",
    "test:email-templates": "npx tsx --test supabase/functions/_shared/email-templates/templates.test.ts",
    "format": "npm run format:write",
    "format:write": "prettier --write --config caremate/.prettierrc --ignore-path caremate/.prettierignore caremate",
    "format:check": "prettier --check --config caremate/.prettierrc --ignore-path caremate/.prettierignore caremate",
    "seed:articles": "npm run seed:articles -w caremate-portal",
    "seed:tips": "npm run seed:tips -w caremate-portal",
    "seed:catalogs": "npm run seed:catalogs -w caremate-portal",
    "postinstall": "patch-package"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "patch-package": "^8.0.1",
    "tr46": "6.0.0"
  },
  "overrides": {
    "tr46": "6.0.0"
  }
}
