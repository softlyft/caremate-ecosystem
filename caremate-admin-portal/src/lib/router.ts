/**
 * App Router navigation that also drives the top loading bar (`nextjs-toploader`).
 * Prefer this over `next/navigation` for `useRouter` in client components.
 */
export { useRouter } from 'nextjs-toploader/app';
