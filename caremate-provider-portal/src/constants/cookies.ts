export const ACTIVE_ORG_COOKIE = 'provider_active_org';
export const PAYER_ACTIVE_ORG_COOKIE = 'payer_active_org';
/** When a user has both provider and payer memberships, prefer this kind on login. */
export const CARE_ACTIVE_KIND_COOKIE = 'care_active_kind';
